[project]
name = "my-chat-gpt-clone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12, <3.14" 
dependencies = [
    "numpy",
    "pandas",
    "torch",
    "datasets",
    "transformers",
    "torchvision"
]